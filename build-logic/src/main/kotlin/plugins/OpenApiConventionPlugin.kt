package plugins

import org.gradle.api.Plugin
import org.gradle.api.Project

class OpenApiConventionPlugin : Plugin<Project> {
    /**
     * Configures OpenAPI-related conventions for the given Gradle project.
     *
     * @param target The Gradle project to configure (the project to which the plugin is applied).
     */
    override fun apply(target: Project) {
        // Configure OpenAPI-related conventions here
        // ...existing code or configuration...
    }
}
