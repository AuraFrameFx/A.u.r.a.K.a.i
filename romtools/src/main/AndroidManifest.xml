<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Example AppInitializer meta-data entry -->
        <meta-data
            android:name="dev.aurakai.auraframefx.romtools.YourInitializer"
            android:value="androidx.startup" />
        <!-- Example foreground service type for Android 14+ -->
        <service
            android:name=".YourForegroundService"
            android:foregroundServiceType="dataSync"
            android:description="@string/rom_service_description" />
    </application>
</manifest>
